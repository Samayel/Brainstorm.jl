language: julia
os:
  - linux
  - osx
julia:
  - release
  - nightly
notifications:
  email:
    recipients:
      - johm@quyo.de
    on_success: always
    on_failure: always
# uncomment the following lines to override the default test script
script:
  - if [[ -a .git/shallow ]]; then git fetch --unshallow; fi
  - julia -e 'Pkg.clone("https://github.com/jlapeyre/DeepConvert.jl.git"); Pkg.build("DeepConvert");'
  - julia -e 'Pkg.clone("https://github.com/Samayel/PrimeSieve.jl.git"); Pkg.build("PrimeSieve");'
  - julia --check-bounds=yes -e 'Pkg.clone(pwd()); Pkg.build("Brainstorm"); Pkg.test("Brainstorm"; coverage=true)'
after_success:
  - julia -e 'cd(Pkg.dir("Brainstorm")); Pkg.add("Coverage"); using Coverage; Coveralls.submit(Coveralls.process_folder())'
